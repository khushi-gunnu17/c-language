// Pointer = A variable that stores the memory address of another variable.

#include <stdio.h>

void main() {
    
    int a = 0;
    a = printf("Pankaj%dSharma");
    printf("Gate Wallah");
    printf("%d", a);

    return;
}